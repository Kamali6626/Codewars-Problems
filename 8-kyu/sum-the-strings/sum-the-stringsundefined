#include <string>
using namespace std;
​
string sum_str(string a, string b) 
{
    if(a == ""){
        a = "0";
    }
​
    if(b == ""){
        b = "0";
    }
​
    return to_string(stoi(a) + stoi(b));
}