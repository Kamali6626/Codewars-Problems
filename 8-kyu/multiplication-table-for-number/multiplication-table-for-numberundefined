#include <string>
using namespace std;
std::string multi_table(int number)
{
    string ans = "";
  for(int i = 1; i <= 10; i++){
    ans += (to_string(i) + " * " + to_string(number) + " = " + to_string(i * number) +(i == 10 ? "" : "\n"));
  }
  return ans;
}