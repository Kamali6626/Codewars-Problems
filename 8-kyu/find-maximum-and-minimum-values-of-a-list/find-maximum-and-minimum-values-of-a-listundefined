#include <vector>
#include <climits>
using namespace std;
​
int min(vector<int> list){
  int min1=INT_MAX;
     for(int val:list){
    if(val<min1)
      min1=val;
  }
  return min1;
}
​
int max(vector<int> list){
 int max1=INT_MIN;
     for(int val:list){
    if(val>max1)
      max1=val;
  }
  return max1;
}